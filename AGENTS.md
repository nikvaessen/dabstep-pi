# Repository Instructions

- Bash scripts must always be checked with `shellcheck` after changes.
- Python scripts must always be linted/formatted with `ruff` and type-checked with `ty` after changes.
